sudo: required
services:
  - docker

before_install:
  - docker build -t wmhgit/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true wmhgit/docker-react npm run test -- --watchAll=false
